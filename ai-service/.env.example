# Service port
PORT=8001

# Hugging Face API key (optional - only needed if using Inference API)
HF_API_KEY=

# OpenAI API key (optional - for reply generation with GPT models)
# Get your key from: https://platform.openai.com/api-keys
# Cost: ~$0.15 per 1M input tokens, $0.60 per 1M output tokens (gpt-4o-mini)
OPENAI_API_KEY=

# Reply generation mode
# Options: "openai" (uses OpenAI API), "local" (uses local flan-t5-base)
# Default: "openai" if OPENAI_API_KEY is set, otherwise "local"
# Recommendation: Use "openai" for production (higher quality, faster)
REPLY_MODE=openai
